// Observable Framework configuration.
// See https://observablehq.com/framework/config for documentation.
export default {
  // The app title; used in the sidebar and page titles.
  title: "CTA El Service Tracker",

  // The path to the source root.
  root: "src",

  // Served from a GitHub Pages *project* site, which lives under a subpath, so
  // asset/link URLs must be prefixed with the repo name. Drop this if a custom
  // domain or user/org page (served at the root) is adopted later.
  base: "/chicago-el-service-tracker/",

  // Some additional configuration options and their defaults:
  // theme: "default", // try "light", "dark", "slate", etc.
  // header: "", // what to show in the header (HTML)
  // footer: "Built with Observable.", // what to show in the footer (HTML)
  // sidebar: true, // whether to show the sidebar
  // toc: true, // whether to show the table of contents
  // pager: true, // whether to show previous & next links in the footer
  // output: "dist", // path to the output root for build
  // search: true, // activate search
};
